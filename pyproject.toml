[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "lognest"
version = "0.3.1"
description = "Opinionated log housekeeping tool with dry-run support"
requires-python = ">=3.9"

[project.scripts]
lognest = "logwash.cli:main"
